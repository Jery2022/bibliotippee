{% extends 'base.html.twig' %}

{% block title %}WordSearch{% endblock %}

{% block body %}
    <h1>WordSearch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ word_search.id }}</td>
            </tr>
            <tr>
                <th>WordKey</th>
                <td>{{ word_search.wordKey }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_word_search_index') }}">back to list</a>

    <a href="{{ path('app_word_search_edit', {'id': word_search.id}) }}">edit</a>

    {{ include('word_search/_delete_form.html.twig') }}
{% endblock %}
